import { shallowMount } from '@vue/test-utils';
import Adddata from  '@/components/AddData.vue';
 
describe('adding data',()=>{
    it('cheking save and contact button',()=>{
        const wrapper=shallowMount(Adddata,{
             
        })
        const submit= jest.fn();
        wrapper.setMethods
    })
})
